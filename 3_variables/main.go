package main

import "fmt"

func main() {
	// var name string = "Aryan"

	//golang allows you to declare variables without specifying the type.Itautomattically infers the type based on the value assigned.
	// var name = "Aryan"
	name := "Aryan"
	var isAdult = true
	fmt.Println(name)
	name = "Aryan Gupta"
	fmt.Println(name)
	fmt.Println(isAdult)

}
